version: "3.9"

services:
  postgres:
    image: postgres:latest
    container_name: inmana-postgres
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=inmana_dev
    ports:
      - "5432:5432"

